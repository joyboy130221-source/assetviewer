window.AppUI=(()=>{function ensure(){if(document.querySelector('#appModal'))return;document.body.insertAdjacentHTML('beforeend',`<div id="appModal" class="modal-backdrop" hidden><div class="modal-card" role="dialog" aria-modal="true"><div class="modal-icon">?</div><h2 id="modalTitle">Confirm action</h2><p id="modalMessage"></p><div class="modal-actions"><button id="modalCancel" class="secondary-button">Cancel</button><button id="modalConfirm" class="primary-button">Confirm</button></div></div></div><div id="toastHost" class="toast-host"></div>`)}
function confirmAction({title='Confirm action',message='Are you sure?',confirmText='Confirm',danger=false}={}){ensure();return new Promise(resolve=>{const root=document.querySelector('#appModal'),ok=document.querySelector('#modalConfirm'),cancel=document.querySelector('#modalCancel');document.querySelector('#modalTitle').textContent=title;document.querySelector('#modalMessage').textContent=message;ok.textContent=confirmText;ok.className=danger?'primary-button danger-button':'primary-button';root.hidden=false;const done=v=>{root.hidden=true;ok.onclick=cancel.onclick=null;resolve(v)};ok.onclick=()=>done(true);cancel.onclick=()=>done(false);root.onclick=e=>{if(e.target===root)done(false)}})}
function toast(message,type='success'){ensure();const n=document.createElement('div');n.className=`toast ${type}`;n.textContent=message;document.querySelector('#toastHost').append(n);setTimeout(()=>n.remove(),4500)}
async function requireAdmin(permission){try{const r=await fetch('/api/auth/me',{cache:'no-store'});if(!r.ok){location.href=`/login.html?next=${encodeURIComponent(location.pathname+location.search)}`;return null}const b=await r.json();if(permission&&!b.user.permissions?.[permission]){document.body.innerHTML='<main class="page-shell"><section class="state-card error"><h2>Access denied</h2><p>Your role does not have permission to open this page.</p><a class="secondary-button" href="/admin.html">Back</a></section></main>';return null}return b.user}catch{return null}}
return{confirmAction,toast,requireAdmin}})();
